<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    android:id="@+id/tab_item_background"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="38dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@drawable/background_for_tab_item"
    android:clipToPadding="false"
    android:elevation="4dp">
    <TextView
        android:id="@+id/textViewInTabItem"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:text="Челленджи"
        android:textAlignment="center"
        android:textAppearance="@style/StandardText3Inter.Black"
        android:layout_marginHorizontal="14dp"
        android:layout_marginVertical="8dp"
        android:textColor="@color/feed_chip_text_color_state"/>

</FrameLayout>